apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: prod
spec:
  type: LoadBalancer
  selector:
    app: flask-app
  ports:
    - name: http
      port: 80          # service port
      targetPort: 5000  # containerPort from deployment
